<UserControl x:Class="WPF_PAR.MVVM.Views.FamiliasView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             xmlns:views="clr-namespace:WPF_PAR.MVVM.Views"
             mc:Ignorable="d" 
             d:DesignHeight="900" d:DesignWidth="1400"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             FontFamily="{DynamicResource MaterialDesignFont}">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <Grid>

        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" CornerRadius="15" Margin="0,0,0,20" Padding="20"
                    Background="{DynamicResource MaterialDesignPaper}">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="1" Opacity="0.1"/>
                </Border.Effect>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Análisis de Familias" FontSize="24" FontWeight="Black" Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Vista General" Opacity="0.6" FontSize="13" 
                                       Visibility="{Binding VerResumen, Converter={StaticResource BoolToVis}}"/>
                            <TextBlock Text="{Binding TituloDetalle, StringFormat='Detalle: {0}'}" 
                                       Opacity="0.8" FontSize="13" FontWeight="Bold" Foreground="{DynamicResource PrimaryHueMidBrush}"
                                       Visibility="{Binding VerDetalle, Converter={StaticResource BoolToVis}}"/>
                        </StackPanel>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <Button Command="{Binding RegresarCommand}" Style="{StaticResource MaterialDesignOutlinedButton}" 
                                Margin="0,0,15,0" Content="VOLVER" Height="40"
                                Visibility="{Binding VerDetalle, Converter={StaticResource BoolToVis}}"/>

                        <materialDesign:Card UniformCornerRadius="20" Padding="5" Margin="0,0,10,0" VerticalAlignment="Center">
                            <ComboBox ItemsSource="{Binding Sucursales}"
                                SelectedItem="{Binding SucursalSeleccionada}"
                                DisplayMemberPath="Nombre"
                                Style="{StaticResource MaterialDesignComboBox}"
                                materialDesign:TextFieldAssist.UnderlineBrush="Transparent"
                                BorderThickness="0" 
                                FontWeight="SemiBold" 
                                Padding="15,8" 
                                Width="180"
                                materialDesign:HintAssist.Hint="Sucursal"/>
                        </materialDesign:Card>
                        <materialDesign:Card UniformCornerRadius="20" Padding="5" Margin="0,0,10,0" VerticalAlignment="Center">
                            <DatePicker SelectedDate="{Binding Filters.FechaInicio}" Width="110" 
                                        materialDesign:TextFieldAssist.UnderlineBrush="Transparent" BorderThickness="0" Padding="10,5"/>
                        </materialDesign:Card>

                        <materialDesign:Card UniformCornerRadius="20" Padding="5" Margin="0,0,10,0" VerticalAlignment="Center">
                            <DatePicker SelectedDate="{Binding Filters.FechaFin}" Width="110" 
                                        materialDesign:TextFieldAssist.UnderlineBrush="Transparent" BorderThickness="0" Padding="10,5"/>
                        </materialDesign:Card>

                        <Button Command="{Binding ActualizarCommand}" Style="{StaticResource MaterialDesignFloatingActionMiniButton}" 
                                Height="40" Width="40" ToolTip="Actualizar Datos">
                            <materialDesign:PackIcon Kind="Magnify"/>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <UniformGrid Grid.Row="1" Columns="3" Margin="0,0,0,20">
                <materialDesign:Card UniformCornerRadius="20" Margin="0,0,15,0" Padding="20">
                    <StackPanel>
                        <TextBlock Text="Venta Total Global (Sin IVA)" Opacity="0.6" FontSize="12" FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding GranTotalVenta, StringFormat=C2}" FontSize="26" FontWeight="Bold" Foreground="#1565C0"/>
                    </StackPanel>
                </materialDesign:Card>

                <materialDesign:Card UniformCornerRadius="20" Margin="0,0,15,0" Padding="20">
                    <StackPanel>
                        <TextBlock Text="Volumen Total" Opacity="0.6" FontSize="12" FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding GranTotalLitros, StringFormat=N0}" FontSize="26" FontWeight="Bold" Foreground="#4CAF50"/>
                        <TextBlock Text="Litros" FontSize="10" Opacity="0.5"/>
                    </StackPanel>
                </materialDesign:Card>

                <materialDesign:Card UniformCornerRadius="20" Padding="0" Cursor="Hand" materialDesign:ElevationAssist.Elevation="Dp2" Background="{DynamicResource MaterialDesignPaper}">
                    <Button Command="{Binding ExportarGlobalCommand}" 
                            Style="{StaticResource MaterialDesignFlatButton}" 
                            Height="Auto" 
                            Content="{Binding TituloReporteCard}" 
                            Foreground="{DynamicResource PrimaryHueMidBrush}" 
                            FontWeight="Bold"/>
                </materialDesign:Card>
            </UniformGrid>

            <Grid Grid.Row="2">

                <ScrollViewer VerticalScrollBarVisibility="Auto" Visibility="{Binding VerResumen, Converter={StaticResource BoolToVis}}">
                    <StackPanel>
                        <materialDesign:Card UniformCornerRadius="20" Margin="0,0,20,20" Padding="20">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <StackPanel Margin="0,0,0,15">
                                    <TextBlock Text="Evolución de Familias" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                                    <TextBlock Text="Comparativa de ingresos a lo largo del tiempo" FontSize="12" Opacity="0.5"/>
                                </StackPanel>


                                <lvc:CartesianChart Grid.Row="1" 
                                    Series="{Binding SeriesEvolucionFamilias}" 
                                    XAxes="{Binding EjeXMeses}"
                                    LegendPosition="Right"
                                    FindingStrategy="CompareAllTakeClosest"
                                    TooltipPosition="Auto"
                                    Margin="10"
                                    ClipToBounds="False"
                                    MinHeight="450"
                                    >

                                    <lvc:CartesianChart.Style>
                                        <Style TargetType="lvc:CartesianChart">
                                            <Setter Property="Visibility" Value="Visible"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsLoading}" Value="True">
                                                    <Setter Property="Visibility" Value="Hidden"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </lvc:CartesianChart.Style>

                                </lvc:CartesianChart>
                            </Grid>
                        </materialDesign:Card>
                        <ItemsControl ItemsSource="{Binding TarjetasFamilias}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <materialDesign:Card Width="260" Margin="0,0,20,20" UniformCornerRadius="15" materialDesign:ElevationAssist.Elevation="Dp2">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="*"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <Border Background="{Binding ColorFondo}" Height="6" CornerRadius="15,15,0,0"/>

                                        <StackPanel Grid.Row="1" Margin="20">
                                            <TextBlock Text="{Binding NombreFamilia}" FontWeight="Bold" FontSize="16" TextTrimming="CharacterEllipsis"/>

                                            <TextBlock Text="{Binding VentaTotal, StringFormat=C2}" FontSize="24" FontWeight="Black" Margin="0,10,0,0" Foreground="{DynamicResource MaterialDesignBody}"/>
                                            <TextBlock Text="{Binding PorcentajeParticipacion, StringFormat='Part: {0:P1}'}" FontSize="12" Opacity="0.6"/>

                                            <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                                <materialDesign:PackIcon Kind="LabelPercentOutline" Width="14" Height="14" 
                                                                         VerticalAlignment="Center" Opacity="0.5" Margin="0,0,5,0"/>
                                                <TextBlock Text="{Binding PrecioPromedio, StringFormat={}{0:C2}}" 
                                                           FontSize="13" FontWeight="SemiBold" Foreground="{DynamicResource MaterialDesignBody}" Opacity="0.8"/>
                                                <TextBlock Text=" / Ltr" FontSize="11" VerticalAlignment="Bottom" Opacity="0.6"/>
                                            </StackPanel>

                                            <Separator Margin="0,10"/>

                                            <Grid>
                                                <StackPanel Orientation="Horizontal">
                                                    <materialDesign:PackIcon Kind="BucketOutline" VerticalAlignment="Center" Margin="0,0,5,0" Opacity="0.5"/>
                                                    <TextBlock Text="{Binding LitrosTotal, StringFormat=N0}" FontWeight="SemiBold"/>
                                                    <TextBlock Text=" L" Opacity="0.6"/>
                                                </StackPanel>
                                            </Grid>
                                        </StackPanel>

                                        <Button Grid.Row="2" Style="{StaticResource MaterialDesignFlatButton}" 
                                                Command="{Binding DataContext.VerDetalleCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding NombreFamilia}"
                                                Content="VER DETALLE" Foreground="{Binding ColorFondo}" HorizontalAlignment="Right"/>
                                    </Grid>
                                </materialDesign:Card>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    </StackPanel>
                </ScrollViewer>


                <ScrollViewer VerticalScrollBarVisibility="Auto" Visibility="{Binding VerDetalle, Converter={StaticResource BoolToVis}}">
                    <Grid Margin="0,0,10,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="380"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <materialDesign:Card Grid.Row="0" Margin="0,0,0,20" Padding="15" UniformCornerRadius="10">
                        </materialDesign:Card>

                        <Grid Grid.Row="1" Margin="0,0,0,20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <materialDesign:Card Grid.Column="0" UniformCornerRadius="20" Margin="0,0,10,0" Padding="10">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>
                                    <TextBlock Text="{Binding TituloGraficoPastel}" FontWeight="Bold" Margin="10" Foreground="{DynamicResource PrimaryHueMidBrush}"/>

                                    <lvc:CartesianChart Grid.Row="1" 
                                Series="{Binding SeriesBarrasDinero}" 
                                YAxes="{Binding EjeYLineasDinero}"
                                XAxes="{Binding EjeXValores}"
                                TooltipPosition="Right">
                                        <lvc:CartesianChart.Style>
                                            <Style TargetType="lvc:CartesianChart">
                                                <Setter Property="Visibility" Value="Visible"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsLoading}" Value="True">
                                                        <Setter Property="Visibility" Value="Hidden"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </lvc:CartesianChart.Style>
                                    </lvc:CartesianChart>
                                </Grid>
                            </materialDesign:Card>

                            <materialDesign:Card Grid.Column="1" UniformCornerRadius="20" Margin="10,0,0,0" Padding="10">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>
                                    <TextBlock Text="Distribución por Volumen (Litros)" FontWeight="Bold" Margin="10" Foreground="{DynamicResource PrimaryHueMidBrush}"/>

                                    <lvc:CartesianChart Grid.Row="1" 
                                        Series="{Binding SeriesBarrasLitros}" 
                                        YAxes="{Binding EjeYLineasLitros}"
                                        XAxes="{Binding EjeXValores}"
                                        TooltipPosition="Right">
                                        <lvc:CartesianChart.Style>
                                            <Style TargetType="lvc:CartesianChart">
                                                <Setter Property="Visibility" Value="Visible"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsLoading}" Value="True">
                                                        <Setter Property="Visibility" Value="Hidden"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </lvc:CartesianChart.Style>
                                    </lvc:CartesianChart>
                                </Grid>
                            </materialDesign:Card>
                        </Grid>

                        <Grid Grid.Row="2" Margin="0,0,0,20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="1*"/>
                                <ColumnDefinition Width="1.5*"/>
                            </Grid.ColumnDefinitions>

                            <materialDesign:Card Grid.Column="0" UniformCornerRadius="20" Padding="0" Margin="0,0,10,0" materialDesign:ElevationAssist.Elevation="Dp2">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="500"/>
                                    </Grid.RowDefinitions>
                                    <Border Background="{DynamicResource MaterialDesignDivider}" Padding="15">
                                        <StackPanel Orientation="Horizontal">
                                            <materialDesign:PackIcon Kind="TrophyVariant" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                                            <TextBlock Text="Productos" VerticalAlignment="Center" FontWeight="Bold" Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                                        </StackPanel>
                                    </Border>
                                    <DataGrid Grid.Row="1" ItemsSource="{Binding ListaDesglose}" AutoGenerateColumns="False" IsReadOnly="True"
                                              Background="Transparent" BorderThickness="0" GridLinesVisibility="None">
                                        <DataGrid.Columns>
                                            <DataGridTextColumn Header="Producto" Binding="{Binding Nombre}" Width="*"/>
                                            <DataGridTextColumn Header="Litros" Binding="{Binding LitrosTotales, StringFormat=N0}" Width="90" FontWeight="Bold"/>
                                        </DataGrid.Columns>
                                    </DataGrid>
                                </Grid>
                            </materialDesign:Card>

                            <materialDesign:Card Grid.Column="1" UniformCornerRadius="20" Padding="0" Margin="10,0,0,0" materialDesign:ElevationAssist.Elevation="Dp2">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="500"/>
                                    </Grid.RowDefinitions>
                                    <Border Background="{DynamicResource MaterialDesignDivider}" Padding="10">
                                        <TextBox Text="{Binding TextoBusqueda, UpdateSourceTrigger=PropertyChanged}" 
                                                 materialDesign:HintAssist.Hint="Buscar cliente..." Width="250" HorizontalAlignment="Left"
                                                 Style="{StaticResource MaterialDesignFloatingHintTextBox}"/>
                                    </Border>
                                    <DataGrid Grid.Row="1" ItemsSource="{Binding DetalleVentas}" AutoGenerateColumns="False" IsReadOnly="True"
                                              Background="Transparent" BorderThickness="0" GridLinesVisibility="None">
                                        <DataGrid.Columns>
                                            <DataGridTextColumn Header="Fecha" Binding="{Binding FechaEmision, StringFormat=dd/MM/yy}"/>
                                            <DataGridTextColumn Header="Cliente" Binding="{Binding Cliente}" Width="*"/>
                                            <DataGridTextColumn Header="Cant" Binding="{Binding Cantidad, StringFormat=N0}" Width="60"/>
                                            <DataGridTextColumn Header="Venta ($)" Binding="{Binding TotalVenta, StringFormat=C0}" Width="90" FontWeight="Bold"/>
                                        </DataGrid.Columns>
                                    </DataGrid>
                                </Grid>
                            </materialDesign:Card>
                        </Grid>
                    </Grid>
                </ScrollViewer>
            </Grid>
        </Grid>

        <views:ParLoadingOverlay Panel.ZIndex="999" 
                                 Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}" />
    </Grid>

</UserControl>