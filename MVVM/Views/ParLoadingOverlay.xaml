<UserControl x:Class="WPF_PAR.MVVM.Views.ParLoadingOverlay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="600">

    <UserControl.Triggers>
        <EventTrigger RoutedEvent="Loaded">
            <BeginStoryboard>
                <Storyboard RepeatBehavior="Forever" AutoReverse="True">
                    <DoubleAnimation Storyboard.TargetName="CapaPintura" 
                                     Storyboard.TargetProperty="Height"
                                     From="0" To="70" Duration="0:0:1.5">
                        <DoubleAnimation.EasingFunction>
                            <QuadraticEase EasingMode="EaseInOut"/>
                        </DoubleAnimation.EasingFunction>
                    </DoubleAnimation>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </UserControl.Triggers>

    <Grid>
        <Border Background="{DynamicResource MaterialDesignPaper}" Opacity="0.85" />

        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <materialDesign:Card UniformCornerRadius="20" Padding="40,30" materialDesign:ElevationAssist.Elevation="Dp8">
                <StackPanel>

                    <Grid Width="150" Height="70" Margin="0,0,0,20" ClipToBounds="True">
                        <TextBlock Text="PAR" FontSize="65" FontWeight="Black" 
                                   Foreground="{DynamicResource MaterialDesignDivider}" 
                                   HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,-10"/>

                        <Grid x:Name="CapaPintura" VerticalAlignment="Bottom" Height="0" ClipToBounds="True">
                            <TextBlock Text="PAR" FontSize="65" FontWeight="Black" 
                                       Foreground="#1565C0" 
                                       HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,-10"/>
                        </Grid>
                    </Grid>

                    <TextBlock Text="Analizando Datos..." FontSize="20" FontWeight="Bold" HorizontalAlignment="Center"/>
                    <TextBlock Text="Preparando tu tablero inteligente" FontSize="13" Opacity="0.5" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                </StackPanel>
            </materialDesign:Card>
        </StackPanel>
    </Grid>
</UserControl>