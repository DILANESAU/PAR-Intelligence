<UserControl x:Class="WPF_PAR.MVVM.Views.CambiarPasswordView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:converters="clr-namespace:WPF_PAR.Converters"
             Width="400" Background="{DynamicResource MaterialDesignPaper}">

    <UserControl.Resources>
        <converters:ArrayConverter x:Key="ArrayConverter"/>
    </UserControl.Resources>
    
    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <materialDesign:PackIcon Kind="ShieldLockOutline" Width="32" Height="32" Foreground="#FF9800" Margin="0,0,10,0"/>
                <TextBlock Text="Cambio de Seguridad" FontSize="22" FontWeight="Bold" VerticalAlignment="Center"/>
            </StackPanel>
            <TextBlock Text="Por políticas de seguridad, es obligatorio que personalices tu contraseña temporal antes de entrar al sistema." 
                       TextWrapping="Wrap" Opacity="0.7"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0,0,0,20">
            <PasswordBox x:Name="txtNuevaPassword" 
                         materialDesign:HintAssist.Hint="Nueva Contraseña" 
                         Style="{StaticResource MaterialDesignFloatingHintPasswordBox}" Margin="0,0,0,15"/>

            <PasswordBox x:Name="txtConfirmarPassword" 
                         materialDesign:HintAssist.Hint="Confirmar Contraseña" 
                         Style="{StaticResource MaterialDesignFloatingHintPasswordBox}"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}" 
                    Content="Cancelar" 
                    Style="{StaticResource MaterialDesignFlatButton}" Margin="0,0,10,0"/>

            <Button Content="Actualizar y Entrar" 
                    Command="{Binding GuardarNuevaPasswordCommand}"
                    materialDesign:ButtonProgressAssist.IsIndicatorVisible="{Binding IsBusy}"
                    materialDesign:ButtonProgressAssist.Value="-1"
                    materialDesign:ButtonProgressAssist.IsIndeterminate="True">

                <Button.Style>
                    <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsBusy}" Value="True">
                                <Setter Property="IsEnabled" Value="False"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>

                <Button.CommandParameter>
                    <MultiBinding Converter="{StaticResource ArrayConverter}">
                        <Binding ElementName="txtNuevaPassword"/>
                        <Binding ElementName="txtConfirmarPassword"/>
                    </MultiBinding>
                </Button.CommandParameter>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>